/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  experimental: {
    // App Router features; leave as default unless you need more.
  }
};

export default nextConfig;
